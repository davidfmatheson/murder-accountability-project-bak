'use strict';

describe('myApp.clearance-rates module', function() {

  beforeEach(module('myApp.clearance-rates'));

  describe('clearance-rates controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var clearanceRatesCtrl = $controller('ClearanceRatesCtrl');
      expect(clearanceRatesCtrl).toBeDefined();
    }));

  });
});
